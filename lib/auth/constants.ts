export const SESSION_COOKIE_NAME = 'nolan_session';

/** Seven days. The owner is the only user; re-login weekly is not a burden. */
export const SESSION_MAX_AGE_SECONDS = 60 * 60 * 24 * 7;

export const LOGIN_PATH = '/login';
